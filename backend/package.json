{
  "name": "meshforge-backend",
  "version": "1.0.0",
  "description": "Secure proxy server between MeshForge frontend and the Replicate API (keeps REPLICATE_API_TOKEN off the client).",
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
